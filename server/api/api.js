const router =  require('express').Router();
// const userRoutes = require('./user/userRoutes.js');
// api router will mount other routers
// for all the  resource. Each resource directory has a resourceRoutes.js
// file with the route ready to go, require them and mount them in their respective routes below.


// router.use('/users', userRoutes);

module.exports = router;
